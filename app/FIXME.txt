Node3D
    1. rotation is not clamped to range [-2pi, 2pi]

EchoX:
    1. transformations doesnt propagate through node. global transform

Renderer:
    1. uses hardcoded shader now, but will require to use object material which will haves it own shader
    2. setting ambient_strength every render
    3. setting ambient_color every frame
    4. setting light_dir every frame

